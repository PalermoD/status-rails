<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <!-- <%= f.submit "Unfollow", class: "unfollow-btn" %> -->
  <%= button_tag(type: "submit", class: "unfollow-btn") do %>
      <i class="fa fa-trash" aria-hidden="true"></i> Unfollow
    <% end %>
<% end %>
